<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('partials/head') %>

<body>
	<!-- Header -->
	<%- include('partials/header') %>
	<!-- /Header -->
<!-- product detail -->
<div class="container products-wrapper">
	<% if (!product) { %>
	  <h2>No existe un auto con ese id.</h2>
	<% } else { %>
  
	  <div class="d-flex align-items-center">
		<h2 class="products-title"><%= product.nombreProducto %></h2>
		<p class="product-detail-description" style="margin-left: 1rem;">
		  <a href="/product/<%= product.id %>/edit">Editar</a>
		</p>
	  </div>
  
	  <div class="product-detail">
		<div class="row">
		  <article class="col-12 col-lg-8 product-detail-info">
			<% if (product.imagenArchivo !== "") { %>
			  <img src="/images/products/<%= product.imagenArchivo %>" alt="<%= product.nombreProducto %>" class="product-detail-img">
			<% } else { %>
			  <img src="/images/products/default-image.png" alt="<%= product.nombreProducto %>" class="product-detail-img">
			<% } %>
  
			<p class="product-detail-description"><%= product.descripcion %></p>
			<p class="product-detail-description">Publicado por: <%= product.usuario_nombre %></p>
		  </article>
  
		  <div class="col-12 col-lg-4">
			<section class="comments">
			  <h3>Comentarios</h3>
			  <% if (comments.length === 0) { %>
				<p>No hay comentarios todavía.</p>
			  <% } else { %>
				<% for (let i = 0; i < comments.length; i++) { %>
				  <article>
					<% if (comments[i].foto !== "") { %>
					  <img src="/images/users/<%= comments[i].foto %>" alt="Foto de <%= comments[i].nombreusuarioComento %>">
					<% } else { %>
					  <img src="/images/users/profile-default.png" alt="Foto por defecto">
					<% } %>
					<div class="data">
					  <p class="user">
						<strong><a href="/users/<%= comments[i].nombreusuarioComento %>">Nombre usuario:</a></strong>
						<%= comments[i].comentario %>
					  </p>
					</div>
				  </article>
				<% } %>
			  <% } %>
			</section>
  
			<form class="form" action="/product/<%= product.id %>/comment" method="POST">
			  <input class="form-input" type="text" name="comentario" placeholder="Agregar comentario">
			  <button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button>
			</form>
			<hr>
		  </div>
		</div>
	  </div>
  
	<% } %>
  </div>
  <!-- /product detail -->
  

	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->

</body>	
</html>